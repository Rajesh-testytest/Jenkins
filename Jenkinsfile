pipeline {
    agent any
    stages {
        stage ('success/failure') {
            steps{
                echo $GIT_BRANCH
                echo GIT_BRANCH
            }
            
        }
    }
}
