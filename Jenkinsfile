pipeline {
    agent any
    stages {
        stage ('success/failure') {
            when {
                branch 'development'
                }
            steps {
                echo "Hello, Rajesh!"
            }
        }
    }
}
